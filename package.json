{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier . --check",
    "remove": "rm -rf dist"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "vite": "^7.2.4"
  }
}
